This location contains 'ontology' documents in the domain resource.geosciml.org. 
These documents contain descriptions of ontology elements (classes, properties) which are typically used as a set, and composed with other resources using the owl:imports property. 

Simon.Cox@csiro.au - 2012-08-29